apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-schemaapp
  labels:
    app: {{ .Release.Name }}-schemaapp
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-schemaapp
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-schemaapp
    spec:
      containers:
      - name: schemaapp
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 80
