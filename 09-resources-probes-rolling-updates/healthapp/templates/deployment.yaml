apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-healthapp
  labels:
    app: {{ .Release.Name }}-healthapp
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-healthapp
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-healthapp
    spec:
      containers:
      - name: healthapp
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 80
