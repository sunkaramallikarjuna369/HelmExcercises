apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-upgradeapp
  labels:
    app: {{ .Release.Name }}-upgradeapp
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-upgradeapp
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-upgradeapp
    spec:
      containers:
      - name: upgradeapp
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 80
