apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-envapp
  labels:
    app: {{ .Release.Name }}-envapp
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-envapp
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-envapp
    spec:
      containers:
      - name: envapp
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 80
