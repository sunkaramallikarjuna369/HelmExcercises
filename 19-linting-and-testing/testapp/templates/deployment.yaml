apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-testapp
  labels:
    app: {{ .Release.Name }}-testapp
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-testapp
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-testapp
    spec:
      containers:
      - name: testapp
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 80
