apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-capapp
  labels:
    app: {{ .Release.Name }}-capapp
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-capapp
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-capapp
    spec:
      containers:
      - name: capapp
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 80
