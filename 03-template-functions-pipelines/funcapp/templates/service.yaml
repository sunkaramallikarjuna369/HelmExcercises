apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-funcapp
  labels:
    app: {{ .Release.Name }}-funcapp
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: {{ .Release.Name }}-funcapp
