apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-statefulapp
  labels:
    app: {{ .Release.Name }}-statefulapp
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-statefulapp
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-statefulapp
    spec:
      containers:
      - name: statefulapp
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 80
